extends layout 

block content 

    h1= title 

    each genre in genreList 
        ul 
            li 
                a(href=genre.url) #{genre.name}

    else 
        p No genre is available in this library